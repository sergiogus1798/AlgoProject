# Builder — pipeline map

Generated by `1_sqx/inspect/dump_project.py` from `/home/sergioguslw/Desktop/SQX/user/projects/Builder/project.cfx`.
Project declares `version="144.2953"`.

> **Read-only artefact.** Regenerate rather than hand-editing. Never edit a
> `project.cfx` on disk while SQX holds the project: SQX rewrites the whole
> file on save and exit, and the change is silently lost.

## Databanks

| databank | view | sync | position |
|---|---|---|---|
| `Results` | Default - Main data | Auto-sync every 1 hour ⚠️ | 0 |
| `Initial population` | Default - Main data | Auto-sync never | 1 |
| `Last generation` | Default - Main data | Auto-sync never | 2 |
| `Strategies to improve` | Default - Main data | Auto-sync never | 3 |
| `Existing portfolio` | Default | Auto-sync never | None |

⚠️ = auto-syncs on a timer. A sync writes memory → disk and **deletes on-disk `.sqx` files not present in memory**. Combined with a `ClearDatabanks` task this is what empties a databank. See `knowhow/02-databanks.md`.

## TL;DR

- **1 tasks**, 0 inactive.
- **Terminal output databanks** (written, never read, never cleared): `Last generation`.

## Databank flow

Which task numbers read, write and clear each databank. A databank that is **cleared while it auto-syncs** loses its `.sqx` files on the next sync.

| databank | written by | read by | cleared by | at risk |
|---|---|---|---|---|
| `Last generation` | 1 | — | — | — |
| `Strategies to improve` | — | 1 | — | — |

## Task order

```
 1. Build                        (Build)
```

## Tasks in detail

### 1. Build

- **type** `Build` · **internal name** `Build` · **file** `Build-Task1.xml`
- **active**: true

**Databanks:** `Strategies to improve` → `Last generation`

- **StrategyType**: `{'type': 'simple', 'additionalCharts': '2', 'templateFile': 'SQ3StrategyTemplateExample.sq4', 'improveType': 'strategy', 'strategyFile': 'D:\\work\\StrategyQuant4\\work_directory\\StrategyQuant\\user\\projects\\Retester\\databanks\\Results\\Strategy 0.1.7.sq4', 'architecture': 'sq4'}`
- **RulesComplexity**: `{'useDifferentSettings': 'false'}`
- **MarketSides**: `{'type': 'both'}`
- **SLPTOptions**: `{}`
- **BuildMode**: `{'generationType': 'genetic-evolution'}`

**Rankings:** MaxStrategies **1000** · StopCondition `databank-full`

**Task-level acceptance conditions:**

| # | on | metric | test | value | source |
|---|----|--------|------|-------|--------|
| 1 | ✅ | `AvgTradesPerMonth` | `>` | **2** | resultType=main, sampleType=127, subresult=30, plType=10, format=Integer |
| 2 | ✅ | `ProfitFactor` | `>` | **1.3** | resultType=main, sampleType=127, subresult=30, plType=10, format=Decimal2 |
| 3 | ✅ | `ReturnDDRatio` | `>` | **4** | resultType=main, sampleType=127, subresult=30, plType=10, format=Decimal2 |

**Cross-checks enabled** (`{'use': 'true', 'evaluateAll': 'true'}`):

#### ↳ Retest with higher precision (`RetestWithHigherPrecision`)

Settings:

- `Precision` = `2`
- `Spread` = `3`

Acceptance conditions:

Matrix/threshold attrs: `CrossCheck=RetestWithHigherPrecision`

| # | on | metric | test | value | source |
|---|----|--------|------|-------|--------|
| 1 | ✅ | `NetProfit` | `>=` | **Net profit** | resultType=RetestWithHigherPrecision, sampleType=127, subresult=30, plType=10, format=Decimal2PL |
| 2 | ✅ | `NumberOfTrades` | `>=` | **# of trades** | resultType=RetestWithHigherPrecision, sampleType=127, subresult=30, plType=10, format=Integer |
| 3 | ✅ | `DrawdownPct` | `<` | **Max DD %** | resultType=RetestWithHigherPrecision, sampleType=127, subresult=30, plType=10, format=Decimal2Pct |

---
