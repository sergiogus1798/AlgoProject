# PortfolioMaster — pipeline map

Generated by `1_sqx/inspect/dump_project.py` from `/home/sergioguslw/Desktop/SQX/user/projects/PortfolioMaster/project.cfx`.
Project declares `version="140.2166"`.

> **Read-only artefact.** Regenerate rather than hand-editing. Never edit a
> `project.cfx` on disk while SQX holds the project: SQX rewrites the whole
> file on save and exit, and the change is silently lost.

## Databanks

| databank | view | sync | position |
|---|---|---|---|
| `Simple strategies` | Default - Main data | Auto-sync never | 1 |
| `Results` | Default - Portfolio | Auto-sync never | 0 |

⚠️ = auto-syncs on a timer. A sync writes memory → disk and **deletes on-disk `.sqx` files not present in memory**. Combined with a `ClearDatabanks` task this is what empties a databank. See `knowhow/02-databanks.md`.

## TL;DR

- **1 tasks**, 0 inactive.
- **Terminal output databanks** (written, never read, never cleared): `Results`.

## Databank flow

Which task numbers read, write and clear each databank. A databank that is **cleared while it auto-syncs** loses its `.sqx` files on the next sync.

| databank | written by | read by | cleared by | at risk |
|---|---|---|---|---|
| `Results` | 1 | — | — | — |
| `Simple strategies` | — | 1 | — | — |

## Task order

```
 1. AutomaticPortfolioBuilder    (AutomaticPortfolioBuilder)
```

## Tasks in detail

### 1. AutomaticPortfolioBuilder

- **type** `AutomaticPortfolioBuilder` · **internal name** `AutomaticPortfolioBuilder` · **file** `AutomaticPortfolioBuilder-Task1.xml`
- **active**: true

**Databanks:** `Simple strategies` → `Results`

---
