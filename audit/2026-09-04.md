# Audit 2026-09-04

**This supersedes the earlier 2026-09-04 run: the SQX scope was narrowed mid-day to export health,
log size and corrupt archives only, so the databank/template/loop findings from the first pass are
dropped from tracking, not carried forward as resolved.**

The one thing worth the owner's attention: a fresh export directory landed in the data root with no
`manifest.json` (`raw/XAUUSD/OOS/2026-09-03/`), and the new IS/OOS report built on top of the wider
`OOS` databank pools 10,000 strategies without the trade-level dedup `2_tasks/CLAUDE.md` itself warns
about — everything else in that report is careful (BH correction, heavy-tail caveats, weak-effect
framing).

Snapshot: `HEAD=08174a7`, working tree dirty (a parallel "documenter" session is rewriting `OPEN.md`,
`CLAUDE.md` and `knowhow/03` while this audit runs; a second session added `2_tasks/analysis/` and
`2_tasks/reports/`, uncommitted). Findings below reflect the tree as read; nothing here was changed
by this audit except this file.

## New since last audit

| severity | area | finding | where | what to do |
|---|---|---|---|---|
| 🟠 | data | `raw/XAUUSD/OOS/2026-09-03/export_data_view.csv` (10,823 bytes, 36 rows) has no `manifest.json` — the only unmanifested export under `raw/` today. Its filename matches no current export script's output pattern (`export_metrics.py` writes `metrics.csv`, `export_trades.py` writes per-strategy trade files), so it isn't reproducible or traceable to a command | `~/Desktop/AlgoData/raw/XAUUSD/OOS/2026-09-03/` | Write a manifest recording where this CSV actually came from, or delete it if it was a stray manual export superseded by `metrics/XAUUSD/OOS/metrics.csv` |
| 🟠 | stats | `2_tasks/reports/is_oos.py`'s new XAUUSD/OOS report (10,000 strategies) does not deduplicate on trade lists before computing any correlation or p-value — the exact trap `2_tasks/CLAUDE.md` names first ("45 of 231 strategies had byte-identical trades under different hashes"). I checked the cheap, insufficient version of this myself (inner-XML identity hash across all 10,000 `.sqx` in `XAUUSD/databanks/OOS`): 10,000/10,000 unique, 0 unreadable — reassuring, but this is precisely the check `CLAUDE.md` says not to trust, since the known duplicates in the old corpus had *different* hashes and identical trades. Trade-level dedup remains undone, and the report doesn't flag the gap | `2_tasks/reports/is_oos.py`, `2_tasks/analysis/metrics.py:load` (no dedup step), report at `~/Desktop/AlgoData/reports/XAUUSD/OOS/2026-09-04/summary.md` | Either export trades for a sample and check for byte-identical lists before trusting the p-values, or add one sentence to `summary.md` naming this as an open assumption |
| 🟡 | docs | `knowhow/05-conditions.md`'s 🤔 `subresult=31 = percentage of matrix combinations passing` rests on a "pairing with `format` (`Decimal2` vs `Decimal2Pct`)" that doesn't exist — no `.vw` file in the install carries a `format` attribute at all. Cheap test run: `grep -ohn 'subresult="[0-9]*"' user/settings/views/databanks/*.vw`. Result contradicts the hypothesis instead of settling it in its favour: `subresult="31"` appears on `NumberOfTrades`, `NetProfit`, `SharpeRatio`, `AvgWin` — none of which are percentages — always paired with `resultType="WalkForwardMatrix"`. The split looks like it tracks `resultType` (single result vs. walk-forward matrix aggregate), not raw-vs-percentage | `knowhow/05-conditions.md` | Retag the inference, drop the `format`-pairing claim, and describe the actual correlate: `subresult=31`/`32` only occur under `resultType="WalkForwardMatrix"` |

## Worse than last audit

Nothing. The one open SQX item this scope still covers — `Infinox_SP500ft_H4_HighPrecision`'s
`config.xml` naming `Retest-Task8.xml` (and four siblings) that the archive lacks — reproduces
identically via `dump_project.py` (`KeyError: There is no item named 'Retest-Task8.xml'`), unchanged
from yesterday, repair built and still waiting on the master GUI to close.

## Resolved since last audit

- 🟢 `docs.export_py_renamed`. `knowhow/06-locations.md` now correctly names `export_trades.py` and
  `export_metrics.py`; the stale `export.py` reference is gone.
- 🟢 `code.sqxfile_no_golden_test` / `tests/test_sqxfile.py` still passes today (`python3
  tests/test_sqxfile.py`, exercised via `tools/daily_audit.py`).

## Unchanged (not re-listed as new; still true today, still open)

- `sqx.sp500ft_missing_task_members` — corrupt archive, repair written, blocked on the GUI closing.
- `code.two_cfx_parsers`, `code.manifest_check_blind_spots`, `code.archived_analyses_unconverted`,
  `docs.phase_claude_over_budget` (`1_sqx/CLAUDE.md` still 41 lines, limit 40) — all reproduce exactly
  as previously recorded; see `audit/state.json` for the notes.
- `stats.atr_stop_in_sample_grid_selected`, `stats.two_population_split_underpowered` —
  `knowhow/06`/`07` still state the ATR-stop PF and the ~129/~11 split without the in-sample/grid or
  n=11/threshold caveats.
- `data.knowhow_results_not_reproducible` — the 231-strategy corpus behind `knowhow/06`/`07` is still
  not reproducible from the current data root; worth noting it is no longer a purely historical
  concern — `metrics/XAUUSD/OOS/metrics.csv` (10,000 rows, feeding today's new report) carries the
  same `code_version: "migrated from AlgoProject_Old, pre-git"`, so a live, actively-used report now
  also sits on non-reproducible source data, not just `knowhow/` prose.

## Dropped from tracking (scope narrowed, not reported as resolved)

Per the new scope, `sqx.template_simple_mismatch`, `sqx.breakout_templates_on_worker_only` (and its
superseded predecessor `sqx.template_catalog_install_mismatch`), and `docs.open1_table_covers_6_of_9`
are project-configuration matters (which template a project names, which databanks a task clears) —
the owner's call, never a finding. Removed from `state.json` silently. `docs.template_grep_offbyone`
(already `fixed`, and itself about `type=simple` counting) dropped for the same reason.
`sqx.tldr_ignores_inactive_tasks` stays, reclassified: the SQX-wiring content (which loops are active,
databank exposure) is out of scope, but `project_map.py` genuinely ignoring `Task@active` is a bug in
our own code, so it is now tracked under `code`, not `sqx` — see `audit/state.json`. It has not been
fixed since yesterday (`databank_flow()`/`tldr()` in `1_sqx/inspect/project_map.py` still don't read
`active`).

## Mechanical

`python3 tools/depmap.py && python3 tools/checks.py`: both clean, 0 problems, 28 files checked.
`python3 tools/daily_audit.py` exits 1, driven by two things: `Infinox_SP500ft_H4_HighPrecision`
failing to render (in scope, unchanged, see above) and the new unmanifested export (in scope, new,
see above). `docs/DEPENDENCIES.md` shows as regenerated (working tree is mid-edit by another session,
not a defect). Line budgets: root `CLAUDE.md` 53/55, `1_sqx/CLAUDE.md` 41/40 (unchanged, over),
`2_tasks/CLAUDE.md` 40/40 (at the limit), `3_strategies` 24/40, `4_portfolio` 14/40.

## OPEN.md entries to add

Not editing `OPEN.md` this run (a documenter agent is working on it in parallel). If it still needs
one after that pass, this is the entry:

> **New: an export directory is missing its manifest, and the databank it's paired with hasn't been
> checked for trade-level duplicates.** `raw/XAUUSD/OOS/2026-09-03/export_data_view.csv` has no
> `manifest.json` and matches no current export script's naming. Separately,
> `2_tasks/reports/is_oos.py`'s 10,000-strategy XAUUSD/OOS report computes every correlation and
> p-value without the trade-list dedup `2_tasks/CLAUDE.md` warns is required — inner-XML identity
> hashing (the check the same file says not to trust) shows 0 duplicates among the 10,000 `.sqx` on
> disk, which does not rule out byte-identical trade lists under different hashes, the failure mode
> that actually happened in the old corpus. Fix: manifest or remove the stray CSV; export trades for
> a sample and check before trusting the p-values, or caveat the report.

