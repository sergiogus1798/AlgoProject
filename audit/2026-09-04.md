# Audit 2026-09-04

**The pipeline maps ignore `active="false"`, so the databank-loss risk table that drives `OPEN.md` 1
is wrong for five of the nine build projects — and the template finding is no longer a suspicion:
measured, zero of ~200 strategies carry their template's blocks.**

Snapshot: `HEAD=c110087`, working tree dirty throughout. A parallel session landed
`project_health.py`, `1_sqx/repair/graft_tasks.py`, `tests/test_sqxfile.py`, `archive_logs.py` and
`template_check.py` while this audit was running, and rewrote `OPEN.md` 3–9 and three `knowhow/`
files. Findings below are as of the end of the run; where that session fixed something mid-audit it
is recorded under Resolved, not New.

## New since last audit

| severity | area | finding | where | what to do |
|---|---|---|---|---|
| 🔴 | sqx | `project_map.databank_flow` and `project_map.tldr` never read a task's `active` attribute, so every map counts disabled tasks as live. Only **AUDJPY, GBPJPY_H1, SP500_H1, XAUUSD** actually have an active unconditional loop; the maps claim nine. At-risk databanks per project: EURUSD **8 → 2**, GBPJPY_H1 **3 → 1**, CADJPY_H1 **7 → 6**. `core/cfx.tasks()` already exposes `active` correctly — the map generator simply does not use it | `1_sqx/inspect/project_map.py:73-108`, every `docs/*-pipeline.md` TL;DR, `OPEN.md` 1, `knowhow/02` | Filter on `active` in both functions, regenerate all 15 maps, then rewrite `OPEN.md` 1's table and drop the 🔬 tag from "every project on this install has that shape" in `knowhow/02` |
| 🟠 | sqx | `XAUUSD_Breakout_H1` lives on the **master** but all five Build tasks name templates under `~/Desktop/SQX_w1/user/settings/StrategyTemplates/breakout_xau/` — the **worker's** install. The master has no `breakout_xau/`. It is the only `type="template"` project on the box, i.e. the only one where the template demonstrably matters, and `bin/clone-sqx-worker.sh` requires deleting `~/Desktop/SQX_w1` to re-run | `docs/XAUUSD_Breakout_H1-pipeline.md` lines 63/204/345/486/627 | Copy `breakout_xau/` into the master's `user/settings/StrategyTemplates/` and repoint the five `templateFile` fields via the worker `-project` API. Until then, do not delete the worker |
| 🟠 | stats | `knowhow/07`'s ATR-stop numbers (PF 2.09 at 0.5×ATR) are **in-sample**: the pool was selected by SQX search over 2008–2017 and the study restricts to 2008–2017, and the reported N is the argmax over an 11-point grid (`N_GRID = 0.5…3.0 step 0.25`) whose winner sits on the grid edge. Neither the selection nor the multiple testing is declared. Only the *relative* degradation under slippage survives — and that rests on a single slippage value (`SLIP_REF = 0.25`), not a curve | `knowhow/07-practices.md`, `archive/studies/atr_stop_study.py:73-74,615,723` | Restate the lesson as the slippage delta only; strike the absolute PF, or requalify it as in-sample and grid-selected |
| 🟠 | stats | `knowhow/06`'s two-population claim: "~129 bar-cap + ~11 signal-exit" accounts for **140 of 231** — 91 strategies (39%) unclassified, the classification threshold unstated, and the median-MAE comparison (1.37 vs 0.95 ×ATR) rests on **n=11**. The proportions are taken over the raw 231, which `knowhow/04` says contains **45 byte-identical trade lists**, and over a pool mixing retest windows (46 of 231 cover only 2018–2023). Tagged 🔬 | `knowhow/06-locations.md`, contradicts `2_tasks/CLAUDE.md` trap 1 | Retag 🤔, state the threshold and the denominator, or redo it on deduplicated trade lists restricted to one window |
| 🟠 | data | Every quantitative claim in `knowhow/06` and `07` derives from the old project's 231-strategy export. The current data root holds **36** strategies' trades. The generating scripts are parked in `archive/studies/` against the old layout (`OPEN.md` 8), so nothing cited in `knowhow/` can be reproduced or challenged today | `~/Desktop/AlgoData/INDEX.md`, `archive/studies/` | Either re-export the 231 corpus with a manifest, or mark the affected `knowhow/` bullets as "from the old project, not reproducible here" |
| 🟡 | code | Two independent parsers of the same `project.cfx` conditions: `core/cfx.py` (golden-tested, walks with `iter`) and `1_sqx/inspect/project_parts.py` (untested, `findall` on direct children only). `dump_project.py` re-opens the ZIP itself rather than using `core.cfx`, so `core.cfx.conditions`/`side` are imported by **nothing but their own test**. The bug in the row above lives in the untested half; the 13-vs-138 miscount corrected yesterday came from the same split | `core/cfx.py:84-125`, `1_sqx/inspect/project_parts.py:34-91`, `1_sqx/inspect/dump_project.py:24-32` | Make `dump_project.py`/`project_map.py` go through `core.cfx`, delete the duplicate, and the golden test starts protecting the maps |
| 🟡 | docs | `knowhow/06` names `1_sqx/export/export.py`; the file is `export_trades.py`. Sole broken path in the whole tree — every other path, command and referenced file in `CLAUDE.md`, `CODESTYLE.md`, the phase files and `knowhow/*` resolves | `knowhow/06-locations.md`, "Tools built here" | Rename it |
| 🟡 | docs | `1_sqx/CLAUDE.md` went over budget during this run: **41 lines, limit 40**. The two lines added were the `repair/` lane and the `project_health.py` / `template_check.py` pointer — both worth having, so something older should go | `1_sqx/CLAUDE.md` | Fold the `repair/` lane row into the lifecycle row, or drop the sentence about worker-built projects being invisible, which `knowhow/03` already carries in full |
| 🟡 | code | `daily_audit.exports_without_manifest()` has two blind spots: it skips any directory that contains a subdirectory, and it never looks outside `raw/`. `~/Desktop/AlgoData/reports/XAUUSD_OOS_explorer.html` carries no manifest and the check cannot see it — after the log archive landed mid-audit with manifests of its own, `reports/` is the only unmanifested output left in the data root. Separately, a stale `docs/DEPENDENCIES.md` is reported but excluded from the exit code | `tools/daily_audit.py:45-56,107` | Walk every directory holding files, cover `derived/` and `reports/`, and fold `dirty` into the exit |

## Worse than last audit

- 🔴 **`sqx.template_simple_mismatch` — was 🤔 suspected, now 🔬 measured.** Confirmed independently
  and in parallel: the other session's `template_check.py` reports **0 of 642** over 8 projects and
  33 databanks, and `OPEN.md` 9 now carries it. My own run, drawn with a different seed and sample
  size, agrees exactly. Sampled on-disk strategies
  against each project's declared template, comparing only the blocks the template *fixes* (random
  blocks excluded, since the builder chooses those):

  | project | template fixes | carrying it |
  |---|---|---|
  | XAUUSD | `Vortex` | 0/60 across OOS, SPP OOS, WFM |
  | AUDJPY | `ROCAboveLevel` | 0/45 |
  | EURUSD | `ROCAboveLevel` | 0/103 across 7 databanks |
  | USDJPY | `ROCAboveLevel` | 0/45 |
  | GBPJPY_H1 | `AroonCrossesAbove` | 0/175 across 13 databanks |
  | SP500_H1 | `HurstExponent` | 0/15 (`Existing portfolio` only — imported, not built) |
  | USDCHF | `CCI` | 0/2 |
  | EURJPY_H1 | `BBWidthRatio` | 0/3 (`Existing portfolio` only) |

  Positive control passes: 40 random XAUUSD strategies yield 0 with no detected blocks and a broad
  indicator mix (`VWAP` 8, `High` 7, `LWMA` 5, `BollingerBands` 5, `Fractal` 4 …), `Vortex` 0 — exactly
  what generic `type="simple"` building produces. The two `Existing portfolio` hits (1/15, 1/3) show the
  detector does find these keys when they are present. **CADJPY_H1 remains unmeasured** — 0 `.sqx` on disk, and its
  `AcceleratorEMALong_Template.sqx` is made entirely of `RandomCondition` blocks, so it fixes nothing
  to look for either way. Residual alternative: a genetic build that mutated a fixed block away in 200/200 strategies —
  implausible, and settled for good the first time `XAUUSD_Breakout_H1` (the `type="template"` control)
  writes a strategy.
- 🟠 **`sqx.template_catalog_install_mismatch` escalates from 🟡.** Yesterday it read as a bootstrap
  nuisance — "the worker has an extra `breakout_xau/` set". It is not: a master project depends on that
  directory. See the second row above.
- 🟡 **`OPEN.md` 1's exposure table covers 6 of 9 build projects.** `CADJPY_H1`, `EURJPY_H1` and
  `USDCHF` are absent, and all three clear a `WFM` databank while auto-sync is on — so the table's own
  "SP500 H1 clears its `WFM` outright" reads as unique when four projects do it. 15 maps now exist; the
  table was written when 6 did.

## Resolved since last audit

- 🟢 `docs.condition_third_schema`. Reverified: `core.cfx.side` handles `Column-Value` on both sides,
  and `knowhow/05`'s counts reproduce exactly — 16 tasks, **138 conditions, 101 active**, Build task
  **49 / 24**.
- 🟢 All 15 renderable projects regenerate **byte-identical** to their committed `docs/*-pipeline.md`.
  No SQX-side drift since yesterday.
- 🟢 `assets/*.yaml` vs `1_sqx/inspect/instruments.py`: **no `sqx_default` has drifted**, and no
  `projects_using_it` list is stale. Only variance is `EURUSD_M1_dukas` (spread 0.3 against the recorded
  0.1), used solely by the corrupt `Infinox_SP500ft_H4_HighPrecision` — not worth a second yaml.
- 🟢 `docs.template_grep_offbyone`, found and fixed inside the same day: `knowhow/03`'s
  `grep -l "'type': 'simple'" docs/*-pipeline.md` returned 10, not 9 (`Builder` is the tenth, with the
  stock relative `SQ3StrategyTemplateExample.sq4`). The parallel session deleted the line while
  rewriting that section. Never opened.
- 🔵 Landed uncommitted during this audit, so credited but not counted as this audit's work:
  `project_health.py` and `1_sqx/repair/graft_tasks.py` (`OPEN.md` 3 — and its finding that the
  wholesale backup swap would have regressed the project name, a databank registration and one task's
  input is a better answer than yesterday's plan), `tests/test_sqxfile.py` with a 28 KB fixture that
  correctly retains its `Results/Main: …` entry so `sqxfile.symbol` is exercised (`OPEN.md` 7),
  `archive_logs.py` (`OPEN.md` 6), `template_check.py` (`OPEN.md` 9).

## Still open, unchanged

`sqx.sp500ft_missing_task_members` (repair built, blocked on the master GUI being closed) ·
`code.archived_analyses_unconverted` · all 17 assets in use still carry `use: null`, so the `core.assets` preflight blocks authoring for
every symbol — unchanged since 2026-09-03, and the reason no new project can be authored today.

## 🤔 entries a cheap test would settle

- `knowhow/03`, "SQX ignores a template under `type=simple`" — **settled today**, see above.
- `knowhow/05`, `subresult=31` is a percentage of matrix combinations. Test:
  `grep -rin "subresult" ~/Desktop/SQX/internal/web/SQUANT/help.txt` and the shipped
  `user/settings/views/databanks/*.vw` for a pairing of 30 and 31 on one metric. No SQX process needed.

## Mechanical

`audit/2026-09-04-mechanical.md` exited **1**. Confirmed against the 07:14 run: driven **solely** by
`Infinox_SP500ft_H4_HighPrecision` failing to render (`KeyError: There is no item named
'Retest-Task8.xml'`). `checks.py` ok, `test_cfx.py` ok, `depmap.py` clean, no export missing a
manifest — nothing else contributed. The parallel session re-ran it at 07:26; that report adds a
`test_sqxfile.py | ok` row and flips `DEPENDENCIES.md` to "stale, regenerated", neither of which
changes the exit driver. Line budgets: root `CLAUDE.md` **52/55**, fine; phase files **41 / 23 / 24 / 14** of 40 —
`1_sqx` is one over, see the last new row. It was 35 when this run started. Both manifests name `"migrated from AlgoProject_Old, pre-git"` rather than
a commit, so no manifest points at a commit that no longer exists.

## Note on method

`docs.export_py_renamed` and the two `stats.*` rows were re-checked against the tree at the end of the
run and still stand. `1_sqx/inspect/project_map.py` was not touched by the parallel session, so
finding 1 is live. Opened as `OPEN.md` 10–13.
