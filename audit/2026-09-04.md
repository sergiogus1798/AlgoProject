# Audit 2026-09-04

**This overwrites today's earlier run (08:37) and its 14:42 mechanical companion
(`audit/2026-09-04-mechanical.md`).** The mechanical checks were re-run and reported clean or
unchanged; this pass adds what a script cannot see, and reviews the new uncommitted work on
`fix/open-issues-2026-09-04` (`tasks/analysis/improvement.py`, `replication.py`,
`tasks/reports/compare.py`, `filters.py`, `docs/manual/02-filtros.md`, `03-comparar-muestras.md`,
`.claude/skills/analysis-generation/`).

The one thing worth the owner's attention: the root `CLAUDE.md` has drifted to 61 lines against its
own 55-line budget — the rename commit that made every path in it correct also pushed it over. The
new filter-sweep and sample-comparison code is genuinely careful (BH correction, bootstrap CIs, a
positive self-test, honest "what this doesn't tell you" sections in both manual pages) but it reads
the same undeduplicated `metrics.csv` the IS/OOS report already sits on, so the open trade-dedup gap
now touches three live reports instead of one.

Snapshot: `HEAD=fce8635`, working tree carries the four new scripts and two manual pages
uncommitted, plus regenerated `docs/DEPENDENCIES.md` and touched `panel.html`. Nothing here was
changed by this audit except this file and the two `OPEN.md` edits noted below.

## New since last audit

| severity | area | finding | where | what to do |
|---|---|---|---|---|
| 🟠 | docs | Root `CLAUDE.md` is now **61 lines**, over its own 55-line budget (it was 53/55 at the last audit, this morning). The rename commit `c6d3645` grew rule 8's wording and the router's `dump_project.py` row past the limit | `CLAUDE.md` | Trim; the two grown entries (rule 8's "neither by its path nor its dotted form", the router's `sqx/inspect/dump_project.py <PROJECT> (OPEN.md)` row) are the ones that grew |
| 🟡 | docs | `tasks/CLAUDE.md` grew from 40/40 (at the limit, noted this morning) to **44/40**, adding the "Two lifecycles" section. Now genuinely over budget, not just at it | `tasks/CLAUDE.md` | Cut ~4 lines, or fold the lifecycle note into an existing section |
| 🟠 | stats | The trade-dedup gap already open on `is_oos.py` now spans two more live reports. `tasks/reports/filters.py` (210-candidate sweep, bootstrap CIs) and `tasks/reports/compare.py` (cross-sample replication) both read the same `metrics/XAUUSD/OOS/metrics.csv` with no trade-list dedup — the trap `tasks/CLAUDE.md` names first. Everything else in both is careful: BH correction via `analysis/correlations.discoveries`, bootstrap intervals reported alongside the point estimate, an explicit self-test in `compare.py`'s own manual page that recovers the expected null and the expected effect | `tasks/reports/filters.py`, `tasks/reports/compare.py`, source `~/Desktop/AlgoData/metrics/XAUUSD/OOS/metrics.csv` | Same fix as the standing item: export trades for a sample and check for byte-identical lists, or caveat all three reports explicitly. Filed as `OPEN.md` issue 16 |
| 🟡 | sqx | Master's live `~/Desktop/SQX/user/log` is **4.4 GB** — `log_2026_08_18.log` alone is 4.66 GB (uncompressed, on disk 17 days after the day it logged, past SQX's claimed 14-day prune window). `sqx/export/archive_logs.py` (OPEN.md issue 6) copies a compressed backup to `AlgoData/logs/` but never deletes the source, so the "4.4 GB → 102 MB" note there describes the backup copy, not the live install. Not urgent — 439 GB free | `/home/sergioguslw/Desktop/SQX/user/log/StrategyQuant/log_2026_08_18.log` | Nothing required; noted so the existing OPEN.md prose isn't read as "the master's log directory shrank" |
| — | docs/code | New work checked and found sound: all four new `__main__` commands (`improvement.py` has none, `filters.py`, `compare.py` do) are named in a manual page (`checks.py` passes, rule 8 held); `filters/manifest.json` and its `improvement.md` on disk match the worked example in `02-filtros.md` line for line (511 survivors, −0.020 median, +0.260 Δ for `CAGR/Max DD % (IS) top 5%`) — the manual wasn't hand-typed against stale output; `compare.py`'s manual page explicitly labels its worked example a self-test on a same-databank split and says so before the reader could mistake it for real replication, which is exactly the trap its own "Cuándo lo usas, y cuándo no" section warns against two paragraphs earlier | `tasks/analysis/improvement.py`, `replication.py`, `tasks/reports/compare.py`, `filters.py`, `docs/manual/02-filtros.md`, `03-comparar-muestras.md` | none — recorded so a future audit doesn't re-check what already passed |

## Worse than last audit

- `stats.is_oos_10k_no_trade_dedup` — same underlying gap, now reported as `OPEN.md` issue 16
  because it spans `filters.py` and `compare.py` too, not just `is_oos.py`.
- `docs.phase_claude_over_budget` — was "sqx/CLAUDE.md 41/40, unchanged, over" only; now root
  `CLAUDE.md` (61/55) and `tasks/CLAUDE.md` (44/40) are also over, both newly.

## Resolved since last audit

Nothing. The scope-narrowing and the fixes recorded this morning (log archiving scheduled, golden
test, `export.py` rename in `knowhow/06`) still hold; nothing newly closed today beyond what the
morning report already listed.

## Unchanged (not re-listed as new; reproduces exactly as recorded in `audit/state.json`)

- `sqx.sp500ft_missing_task_members` — corrupt archive, repair built, blocked on the GUI closing.
- `data.export_missing_manifest` — `raw/XAUUSD/OOS/2026-09-03/export_data_view.csv`, no manifest.
- `code.two_cfx_parsers`, `code.manifest_check_blind_spots`, `code.archived_analyses_unconverted`,
  `code.tldr_ignores_task_active`.
- `stats.atr_stop_in_sample_grid_selected`, `stats.two_population_split_underpowered`,
  `data.knowhow_results_not_reproducible`.
- `docs.subresult31_format_claim_unsupported` — `knowhow/05`'s 🤔 still uncorrected; the cheap test
  that settles it (`grep -ohn 'subresult="[0-9]*"' user/settings/views/databanks/*.vw`, already run)
  shows the split tracks `resultType`, not a `format` attribute that doesn't exist on any `.vw`.

## Mechanical (re-run, not re-described in detail — see `audit/2026-09-04-mechanical.md`)

`python3 tools/depmap.py && python3 tools/checks.py`: both clean, 0 problems, 33 files checked.
`docs/DEPENDENCIES.md`: regenerated (was stale, now current in the working tree). Project that fails
to render: `Infinox_SP500ft_H4_HighPrecision` (unchanged, repair blocked on GUI). Export without a
manifest: `raw/XAUUSD/OOS/2026-09-03` (unchanged). Assets in use with cost undecided: 17 symbols,
matched 1:1 against `python3 -m sqx.inspect.instruments` today — no `sqx_default` drift found in any
of the 17 `assets/*.yaml` files, so this list is unchanged and not a new finding.

## OPEN.md entries added this run

- **Issue 16** (new, 🟠): unmanifested export + the trade-dedup gap now spanning `is_oos.py`,
  `filters.py` and `compare.py`.
- **Note added to issue 6**: clarifies the master's live log directory is still 4.4 GB; the archiver
  backs it up, it does not shrink it.
