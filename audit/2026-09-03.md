# Audit 2026-09-03 — baseline

**Nine projects may have been generating strategies from a template SQX never applied.** That is the
one finding worth acting on. Everything else here is either already tracked or cosmetic.

This is the baseline audit, produced while the project was rebuilt rather than by a scheduled run of
the `auditor` agent. Later audits report only what changed against `audit/state.json`.

## New

| severity | area | finding | where | what to do |
|---|---|---|---|---|
| 🔴 | sqx | Nine projects declare `StrategyType type="simple"` while naming a real template file. The reported consequence is that the template is ignored | `OPEN.md` 9, `knowhow/03` | Take one strategy from a databank those projects wrote and check whether its rules use the template's blocks. Cheap, and settles it |
| 🟠 | sqx | `Infinox_SP500ft_H4_HighPrecision` is missing five task XML members that `project_backup.cfx` still holds — the reason the GUI drops it silently | `OPEN.md` 3 | Repair when the lifecycle lane is free: close SQX, back up, swap in the backup |
| 🟠 | docs | Acceptance conditions have a third schema: both sides can be columns. A parser assuming a numeric right side crashes on the SPP and WFM tasks | `knowhow/05` | Fixed in `core/cfx.py`; documented |
| 🟡 | code | `core.sqxfile` has no golden test, so a silent change in how symbols or hashes are read would surface only as strange results | `OPEN.md` 7 | Decide on a fixture: commit one small `.sqx`, or point the test at `config/machine.yaml` |
| 🟡 | code | Eight migrated analysis scripts are parked in `archive/`, written against the old data layout | `OPEN.md` 8 | Rewrite over `core/` on first reuse, not before |
| 🟡 | sqx | `sqx-strategy-template`'s catalog was built from the master while the recorded install is the worker. The block and group catalogs are byte-identical between the two, so only the template set actually differs — the worker has an extra `breakout_xau/` set | this file | Decide which install the authoring skills should see, then re-bootstrap |

## Corrected

- An earlier note said XAUUSD's Build task had 13 conditions with 6 active. Counted with `core/cfx.py`:
  the project holds **138 conditions across 16 tasks, 101 active**, and the Build task alone holds 49
  with 24 active. The old figure was counting one block of them.

## Already tracked, unchanged

`OPEN.md` 1 (databank clearing), 2 (accepted loss, closed), 4 (project versions older than the app),
5 (mojibake template path), 6 (logs pruned to 14 days).

## Mechanical checks

| check | result |
|---|---|
| `tools/checks.py` | 18 files, 0 problems |
| `tools/depmap.py` | regenerates `docs/DEPENDENCIES.md` unchanged |
| `tests/test_cfx.py` | ok |
| Root `CLAUDE.md` budget | 52 of 55 lines |
| Pipeline maps | 15 of 16 projects render; the sixteenth is `OPEN.md` 9's corrupt project |
| Data root | 4 exports, all carrying a manifest |
