# Audit 2026-09-11

`strategies/CLAUDE.md` is now 57 lines against its own 40-line budget (was 24 four days ago) —
everything else that changed today, including a large body of new, uncommitted analysis code, holds
up on inspection.

Mechanical half already run by `tools/daily_audit.py`: see `audit/2026-09-11-mechanical.md`. Not
repeated here except where it changes the interpretation of a tracked finding.

Branch `fix/open-issues-2026-09-04` carries a lot of uncommitted work: six new `core/` modules
(`bars`, `optprofile`, `sqxstats`, `trades`, `wfmatrix`, `wftrades`), a full `strategies/crossmarket/`
and `strategies/monteCarlo/` study each with its own manual page and `POSSIBLE_IMPROVEMENTS.md`, four
new export scripts (`export_bars/retest/spp/wfm.py`), `sqx/curate/apply_verdict.py`, a repair script
(`sqx/repair/graft_tasks.py`), eight new manual pages (02–09) and three new skills. I read all of it
against `CODESTYLE.md` (including its new rule 5, added this session, on modelling/execution/
inference boundaries), `CLAUDE.md` rule 8 (manual page per `__main__`) and rule 9 (no absolute paths
outside `core/paths.py`): no violations found. `checks.py` already reported clean. The statistical
write-ups (`04-decaimiento.md`, `07-montecarlo.md`) are unusually honest about their own limits —
both declare the search-multiplicity problem explicitly, state sample sizes and what they don't
support, and the three placeholder Monte Carlo thresholds are already tracked (`OPEN.md` issue 19).
This is the strongest audit outcome available short of "nothing to report": a lot of surface area
added, and it was checked and passed.

## New since last audit

| severity | area | finding | where | what to do |
|---|---|---|---|---|
| 🟡 | docs | `strategies/CLAUDE.md` grew from 24/40 (2026-09-04) to **57/40** lines, adding the study-folder table for `crossmarket/` and `monteCarlo/`. Genuinely over budget now, not a phase file that happens to be large | `strategies/CLAUDE.md` | Trim to fit, or split the "Traps" and "Translation" sections into `knowhow/` |
| 🟡 | code/governance | `.claude/settings.json` (edited this session) allows any `python3:*` command without asking, then re-gates `sqx.curate.apply_verdict --apply` (rewrites a live databank) back into `ask`. `sqx.repair.graft_tasks --apply` (rewrites a project archive, `OPEN.md` issue 3) is the same class of destructive action and takes the same flag, but has no matching entry, so it now runs unprompted. Its own `/proc` guard still blocks it while the master GUI is up, so this is not exploitable today, just inconsistent | `.claude/settings.json` | Add `graft_tasks` to `ask` alongside `apply_verdict`. Filed as `OPEN.md` issue 20 |
| 🟠 | stats | The trade-list dedup gap (`OPEN.md` issue 16) widens again: `tasks/reports/decay.py` (new) reports "834 estrategias → 4 supervivientes" and argues that 4-of-834 survivors is roughly what chance alone produces — a multiplicity argument whose 834-strategy denominator has never been checked for duplicate trade lists under different `.sqx` hashes (the exact failure mode found in the old 231-strategy corpus). The manual page discloses the *search*-multiplicity gap but not this one | `tasks/reports/decay.py`, `docs/manual/04-decaimiento.md` | Same fix as the standing item: export trades for a sample and check for byte-identical lists |

## Worse than last audit

- `docs.phase_claude_over_budget` — `strategies/CLAUDE.md` (57/40) joins the list. Root `CLAUDE.md`
  (61/55) and `sqx/CLAUDE.md` (41/40) are unchanged; `tasks/CLAUDE.md` came back into budget (see
  Resolved).
- `stats.is_oos_10k_no_trade_dedup` (`OPEN.md` issue 16) — now spans four reports
  (`is_oos.py`, `filters.py`, `compare.py`, `decay.py`), the last via a different data path (raw
  `.sqx` rather than `metrics.csv`) but the same unchecked assumption.

## Resolved since last audit

- `tasks/CLAUDE.md` is back to 40/40 (was 44/40 in an uncommitted afternoon pass on 2026-09-04) — the
  "Two lifecycles" wording was cut down, not removed.
- `raw/XAUUSD/OOS/2026-09-03/` (the unmanifested export named in `OPEN.md` issue 16) no longer exists
  on disk. Half of issue 16 closed; the trade-dedup half remains and widened (see above).

## Unchanged (reproduces exactly as recorded in `audit/state.json`, not re-described)

- `sqx.sp500ft_missing_task_members` — `dump_project.py Infinox_SP500ft_H4_HighPrecision` still
  raises `KeyError: There is no item named 'Retest-Task8.xml'`. Repair built, blocked on the GUI.
- `code.manifest_check_blind_spots` — same root cause (`exports_without_manifest()` only checks one
  parent directory up), now producing far more noise: the ~70 "missing manifest" hits under
  `WFM_Stability/2026-09-10/wfm/trades/<strategy>/` and the three under `Retest_Markets_-_Family` all
  have a real `manifest.json` two directories up (`wfm/manifest.json`,
  `Retest_Markets_-_Family/2026-09-09/manifest.json`). Confirmed by listing both. Not new data-root
  hygiene problems — a scale-up of an already-tracked checker bug.
- `code.two_cfx_parsers`, `code.archived_analyses_unconverted`,
  `docs.subresult31_format_claim_unsupported`, `stats.atr_stop_in_sample_grid_selected`,
  `stats.two_population_split_underpowered`, `data.knowhow_results_not_reproducible`.
- Assets: all 17 `assets/*.yaml` `sqx_default` values still match `python3 -m sqx.inspect.instruments`
  exactly (spread, point value, commission checked by hand for XAUUSD; mechanical script confirms 0
  drift across all 17). Same 17 symbols still carry `use: null` under live projects — an owner
  decision pending, not new.
- `OPEN.md` issues 1, 3, 4, 9, 10, 11, 12, 13, 17, 19 — no evidence of change today; none rechecked in
  depth beyond what the mechanical run and this pass already covered.

## Mechanical (see `audit/2026-09-11-mechanical.md` for full detail)

`checks.py`, `test_cfx.py`, `test_sqxfile.py`: ok. `docs/DEPENDENCIES.md` was stale, regenerated.
Line budgets: root `CLAUDE.md` 61/55 (unchanged, over), `sqx/CLAUDE.md` 41/40 (unchanged, over),
`tasks/CLAUDE.md` 40/40 (fixed), `strategies/CLAUDE.md` 57/40 (new, over — see above),
`portfolio/CLAUDE.md` 14/40. `Infinox_SP500ft_H4_HighPrecision` still fails to render. ~70 exports
flagged without a manifest — all false positives from the known checker blind spot (see above), not
new hygiene problems. 17 assets in use with cost still undecided, unchanged, no `sqx_default` drift.

## OPEN.md entries added or edited this run

- **Issue 16**: rewritten — the unmanifested export is resolved, the trade-dedup gap widened to a
  fourth report (`decay.py`).
- **Issue 20** (new, 🟡): `sqx.repair.graft_tasks --apply` is not gated in `.claude/settings.json` the
  way `sqx.curate.apply_verdict --apply` is.
